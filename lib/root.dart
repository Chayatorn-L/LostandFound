import 'package:flutter/material.dart';

import 'main.dart';

void main() {
  runApp(const LostandFoundKMUTT());
}

class LostandFoundKMUTT extends StatelessWidget {
  const LostandFoundKMUTT({super.key});

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
     return const Main();
  }
}

